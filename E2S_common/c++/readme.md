c++ version
## Install
using visual c++ 15 (or advanced version) to generate the project. You can directly creat a new project from the whole code. (choose `New` in `File` button, then open `Project From Existing code`)
